# github_actions_demo

## Run tests

Install the dependencies and run pytest:

```bash
python -m pip install -r requirements.txt
pytest
```

The configured run writes `reports/pytest-report.html` and
`reports/pytest-report.json`.