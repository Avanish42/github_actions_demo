name : Test Workflow

on:
    workflow_dispatch
   
env:
    TZ: 'Asia/Kolkata'

jobs:
    J1:
        runs-on: ubuntu-latest 
        steps:
            - name: echo name
              run: echo "Avnaish" 

            - name : Echo Test
              run : echo $(date)

    
